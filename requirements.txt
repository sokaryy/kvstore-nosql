# Core
# (stdlib: json, socket, threading, http.server, etc.)

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: for word embedding index
numpy>=1.24.0

# Optional: for simple sentence embeddings (no external API)
# Using sklearn or numpy for demo - or sentence-transformers for real embeddings
scikit-learn>=1.3.0
